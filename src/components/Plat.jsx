const Plat = ({ name, percentage }) => {
        return (
        <div className="progress-bar">
        <h3>{nom}</h3>
        <img src={url}
        ></img>
        <span>{prix}euros</span>
        </div>
        );
        };
        export default Plat;
